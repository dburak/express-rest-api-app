extends layout
block content
    h2 Customers
        p There are #{customers.length} orders waiting.
        ol
            each customer in customers
                li
                    a(href=`/customers/${customer.name}`) #{customer.name}
